object ShapeShifter
    def square(side: Int(32), thickness: Int(32)): Int(32) {
        if (side <= 0 || thickness <= 0) then
            Std.printString("Side and thickness must be positive");
        end if
        
    }
end ShapeShifter
